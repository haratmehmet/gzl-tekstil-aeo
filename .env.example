# Database Connection Configuration
# Next.js runs on the host (outside Docker) and connects to PostgreSQL inside Docker.
# PostgreSQL host port is mapped to 5433.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/gzlaeo_db?schema=public"
